module.exports = {
    name: "Pull Bot Model",
    route: "/api/bot/models/pull",
    method: "POST",
    action(request, response){
        // Post ile ID gönderildi.

        // Veritabanından ID'ye göre veriyi
        // çekiceksin

        
    }
}